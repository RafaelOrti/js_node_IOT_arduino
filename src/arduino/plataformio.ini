[env:esp32]
platform = espressif32
board = esp32dev
framework = arduino

lib_deps =
  PubSubClient
  WiFi

upload_port = COMX   ; Puerto COM utilizado para la carga del firmware (reemplazar COMX con el puerto correcto)
